<h1>Index page</h1>

<a>Add a new photo </a>

<% @list_of_photos.each do |a_photo| %>
<img src = "<%= a_photo.source %>">
<a class = "btn btn-secondary btn-block">Show Details</a>
<a class = "btn btn-secondary btn-block">Edit</a>
<a  href="/delete_photo/<%= a_photo.id %>" class = "btn btn-secondary btn-block">Delete</a>
<% end %>
